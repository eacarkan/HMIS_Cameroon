# components/tables

Reusable, presentational table/list components (shadcn + TanStack Table) for the
data grids an HMIS lives on — patient lists, invoice lists, the audit log (06 §10).
Compact rows, tabular numerals, right-aligned amounts; no business logic.

_Empty in the foundation (Steps 1-2); populated from Step 6 onward._
