# Findings Register — MinSANTE HMIS / SIGH-DME (Phase 1A → Phase 5)

Baseline `cee1557`. Severity: **Critical** (blocks synthetic-RC acceptance or major privacy/safety/security defect) · **High** (fix before real-data pilot) · **Medium** (fix before extended UAT/pilot) · **Low** (cleanup) · **Advisory** (improvement).

**Headline:** **no Critical findings.** The synthetic release candidate is acceptable; the two **High** items (F-01, F-02) are gating for *real cash / real data / production*, not for synthetic demo or UAT.

## Blocking legend (per finding)
Columns mark whether the finding **blocks**: `RC` = synthetic release-candidate acceptance · `Demo` = controlled synthetic demo/DOSTS · `UAT` = controlled synthetic UAT · `Pilot` = real-data pilot / Gate 7 · `Prod` = production.

| ID | Sev | Area | Finding | Evidence | Risk | Recommendation | Owner | Timing | RC | Demo | UAT | Pilot | Prod |
|---|---|---|---|---|---|---|---|---|:--:|:--:|:--:|:--:|:--:|
| **F-01** | **High** | Financial integrity | `recordPayment` is **not transactional**: `createPayment(...)` then `updateInvoiceStatus(...)` are separate awaited writes, and the balance check uses a **stale read** (`invoiceBalance(invoice)` read at the top, no row lock). | `server/services/billing-service.ts` (createPayment @~L20 → updateInvoiceStatus @~L32, no `$transaction`); `server/db/invoices.ts` has 0 `$transaction`. | Under concurrency (double-click, retries, two tills) two payments can each pass `amount ≤ remaining` and both commit → **over-payment**; a crash between writes leaves a payment recorded with a **stale stored invoice status**. This is the one financial path that did **not** receive the H2/3F-1 transactional treatment given to cancellation/refund/shift. | Wrap create-payment + status-update in one interactive `$transaction`; re-derive `remaining` **inside** the tx under a row lock (mirror `accrueEmergencyDebtTx` / stock guards); add a concurrency regression test. | Software | Before real cash | — | — | — | ✅ | ✅ |
| **F-02** | **High** | Auth / security | **Account lockout is policy-only, not enforced.** `lib/account-security.ts` defines lockout policy but there are **no** persisted counter columns and **no** enforcement in the login/authorize path. | No `failedLogin*/lockedUntil/lockout` columns in `prisma/schema.prisma`; no lockout check in `auth-service`/`lib/auth`. (Consistent with the Phase 1A note that lockout counters were "proposed, not created".) | No brute-force / credential-stuffing protection on login. Acceptable for a synthetic single-user demo; a real security gap for any real-data/production deployment. | Add persisted failed-attempt counter + `lockedUntil` (additive migration), enforce in the credentials authorize path, audit lock/unlock. Pair with rate-limiting at the edge (infra). | Software (+infra) | Before production / real data | — | — | — | ✅ | ✅ |
| **F-03** | Medium | RBAC / UI | Page-level gating uses the **cross-hospital role union** (`can(actor.roles, …)`) in ~91 places vs 10 pages resolving per-hospital roles. A user with different roles at different hospitals may *see* a page/nav item they cannot actually use at the active hospital. | `grep can(actor.roles` → 91 occurrences in `app/`; service layer (250 `requireCapability`) is authoritative per-hospital. | **Low actual risk** (service denies the action; no data leak), but an inconsistent UX and a defense-in-depth gap; matters more as multi-hospital users appear. Documented as a known limitation since Phase 3. | Converge page/nav gating to per-hospital (`canAtHospital(rolesByHospital, activeHospitalId, cap)`); keep the service layer as the real control. | Software | Before extended UAT | — | — | — | ⚠ | ⚠ |
| **F-04** | Medium | QA credibility | **No test-coverage instrumentation.** 758 vitest + 65 e2e, but no coverage provider/threshold is configured, so line/branch coverage is unmeasured. | No `coverage` config in `vitest.config.ts`/`package.json`. | Large green suite ≠ known coverage. For a system heading toward a real-data pilot, unmeasured coverage weakens assurance (esp. on error/negative branches). | Enable `vitest --coverage` (v8), publish a baseline, set a floor for `lib/` + `server/services`+`server/db`; do not gate the RC on a number, but report it. | Software | Before real-data pilot | — | — | — | ⚠ | ⚠ |
| **F-05** | Medium | Performance | **No load / concurrency-at-scale testing.** Phase 5D was a query-review + a scoping test, not a benchmark; pagination exists on some lists but not systematically; no dataset-size or latency evidence. | `5D_performance-query-hardening.md`; audit list bounded, but no perf transcript / large-seed test. | Behaviour at realistic multi-hospital data volumes and concurrent cashiers/pharmacists is unproven. | Add a large synthetic dataset + basic latency/throughput measurements on the heaviest pages (dashboard, reports, patient search, audit) before a real pilot. | Software | Before real-data pilot | — | — | — | ⚠ | ⚠ |
| **F-06** | Medium | Security assurance | **No external threat model / penetration test.** 5G is explicitly app-level only (RBAC/central/flags/audit review) — appropriate, but not an independent security assessment. | `5G_security-access-review.md` (self-scoped "app-level only, not external cybersecurity audit"). | App-level review is necessary but not sufficient for production authorization; a cyber baseline is a separate procurement item. | Commission an independent cybersecurity baseline + pen-test as part of the broader implementation project (out of software scope) before production/Gate 7. | Ministry / supplier | Before production / Gate 7 | — | — | — | ⚠ | ✅ |
| **F-07** | Low | Data integrity (4G) | Cross-hospital patient pairing is prevented at the **service layer** (generation scans one hospital) but **not** by a DB constraint on `PatientMatchCandidate`. | `patient-match-service` blocking is hospital-scoped; no composite FK/trigger forbidding cross-hospital pairs. Tested via integration. | Defense-in-depth only; no live path creates cross-hospital pairs. A future direct writer could bypass the intent. | Optional: a DB CHECK/trigger asserting both patients share `hospitalId` (belt to the tested service guard). | Software | Optional / before pilot | — | — | — | — | — |
| **F-08** | Low | Error handling | The 5E error sanitiser is a **heuristic allow/deny regex**; a novel technical message not matching the leak patterns could pass through, and a clean French message containing a blocked token could be over-suppressed. | `lib/errors.ts` `LEAKY` regex; conservative generic fallback. | Low — the default collapses unknowns to a safe message; residual risk is a rare mis-classification. | Prefer a typed `UserError`/`ValidationError` class thrown by services (pass-through) with *everything else* → generic, replacing the heuristic over time. | Software | Optional | — | — | — | — | — |
| **F-09** | Low | Runtime (library) | Residual `pg` `client.query()`-while-busy deprecation warning (1 per process; **no application stack frame**) from `@prisma/adapter-pg` during interactive transactions. The one app-level cause was fixed in 5D. | e2e `[WebServer]` log: exactly 1 `DeprecationWarning`; `KNOWN_ISSUES.md` #1. | Non-blocking; no functional impact; will disappear on a `pg`/adapter upgrade. | Track for a driver/`pg`-9 upgrade; no action needed for the RC. | Software | Deferred | — | — | — | — | — |
| **F-10** | Advisory | i18n | English is a **progressive overlay** — only the declared `BILINGUAL_NAMESPACES` (22) are guaranteed bilingual; other namespaces fall back to French in English mode. | `tests/unit/i18n-parity.test.ts` (declared namespaces only). | Acceptable for a French-first deployment; English users see mixed FR/EN outside declared namespaces. | If English is a real target audience, expand bilingual coverage; otherwise document English as partial. | Software | Optional | — | — | — | — | — |
| **F-11** | Advisory | Accessibility | No accessibility (WCAG) evaluation performed. | No a11y check in QA. | Public-sector UIs often carry accessibility expectations. | Add a lightweight a11y pass (axe) before a real pilot. | Software | Before pilot (optional) | — | — | — | — | — |
| **F-12** | Advisory | Financial (emergency) | Emergency debt for an **unpriced** dispense uses a documented **placeholder** amount. | `2H`/`3F-2` logs. | By design for the synthetic demo; real pricing must back real debts. | Wire real tariffs for emergency-dispense debt before real cash. | Software | Before real cash | — | — | — | ⚠ | ⚠ |
| **F-13** | Advisory | Patient matching | 4G uses **exact-match blocking** (name/DOB/phone/guardian) — precision-favoured; will under-detect real-world spelling/format variants. | `lib/patient-match.ts` thresholds. | Appropriate for warning-only readiness; would miss fuzzy duplicates in real data. | If moved toward a real pilot, add conservative fuzzy signals with human review (never auto-merge). | Software | Before pilot (optional) | — | — | — | — | — |
| **F-14** | Advisory | Privacy tooling | `check:privacy` is self-described as a **lightweight** scan (not a full secret/DLP scanner). | `scripts/check-privacy.ts` header. | Sufficient as a guardrail for synthetic data; not a substitute for a real secret-scanning pipeline. | Add CI secret-scanning (gitleaks/trufflehog) before handling any real credentials. | Software / infra | Before pilot | — | — | — | — | — |

**Legend:** ✅ blocks · ⚠ should be resolved (does not hard-block) · — does not block.

## Summary by severity
- **Critical:** none.
- **High (2):** F-01 (payment atomicity), F-02 (lockout enforcement) — both block **real-data pilot / production**, neither blocks synthetic RC/demo/UAT.
- **Medium (4):** F-03 (UI union gating), F-04 (coverage), F-05 (load testing), F-06 (external security) — resolve before extended UAT / real pilot.
- **Low (3):** F-07, F-08, F-09.
- **Advisory (5):** F-10 … F-14.
